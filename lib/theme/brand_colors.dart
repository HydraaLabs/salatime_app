import 'package:flutter/material.dart';

/// SalaTime forest green, shared with the website's config/brand.php palette.
class BrandColors {
  static const primaryValue = 0xFF2F5233;
  static const darkAccentValue = 0xFF8DB891;
  static const primary = Color(primaryValue);
  static const secondary = Color(0xFF4C7A50);
  static const light = Color(0xFF6B9A6E);
  static const darkAccent = Color(darkAccentValue);
  static const soft = Color(0xFFEFF3EA);
  static const canvas = Color(0xFFF3F5EE);
}
